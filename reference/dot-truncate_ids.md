# Truncate a vector of IDs for display in warnings

Truncate a vector of IDs for display in warnings

## Usage

``` r
.truncate_ids(ids, max_show = 5)
```

## Arguments

- ids:

  Character vector of IDs.

- max_show:

  Maximum number to show before truncating.

## Value

Formatted string.
