# R reference: spherical parallel distance

R reference: spherical parallel distance

## Usage

``` r
.r_d_par_sph(si, ei, sj, ej)
```

## Arguments

- si, ei, sj, ej:

  Segment endpoints (each c(lon, lat)).

## Value

Parallel distance in meters (double).
