# Remove consecutive duplicate points within each trajectory

Remove consecutive duplicate points within each trajectory

## Usage

``` r
.remove_consecutive_duplicates(df)
```

## Arguments

- df:

  data.frame with columns traj_id, x, y.

## Value

List with data (filtered df) and n_removed (integer).
