# Validate inputs for distance functions

Validate inputs for distance functions

## Usage

``` r
.validate_dist_inputs(si, ei, sj, ej, method)
```

## Arguments

- si, ei, sj, ej:

  Point vectors to validate.

- method:

  Distance method string.

## Value

NULL (invisible). Throws errors on invalid input.
