# R reference: euclidean segment length

R reference: euclidean segment length

## Usage

``` r
.r_seg_len_euc(s, e)
```

## Arguments

- s:

  Start point (length-2 numeric).

- e:

  End point (length-2 numeric).

## Value

Euclidean distance (double).
