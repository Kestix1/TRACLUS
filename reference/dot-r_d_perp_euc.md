# R reference: euclidean perpendicular distance (Paper Def. 1)

R reference: euclidean perpendicular distance (Paper Def. 1)

## Usage

``` r
.r_d_perp_euc(si, ei, sj, ej)
```

## Arguments

- si, ei, sj, ej:

  Segment endpoints (each length-2 numeric).

## Value

Perpendicular distance (double).
