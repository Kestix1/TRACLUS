% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tc_estimate_params.R
\name{tc_estimate_params}
\alias{tc_estimate_params}
\title{Estimate clustering parameters from data}
\usage{
tc_estimate_params(
  x,
  eps_grid = NULL,
  sample_size = 200L,
  w_perp = 1,
  w_par = 1,
  w_angle = 1,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{tc_partitions} object created by \code{\link[=tc_partition]{tc_partition()}}.}

\item{eps_grid}{Optional numeric vector of candidate eps values to
evaluate. If \code{NULL} (default), an intelligent grid of 50 values
is generated from a random sample of pairwise distances (5th to
95th percentile).}

\item{sample_size}{Integer number of segments to sample for pairwise
distance computation (default 200). Ignored when \code{eps_grid} is
provided explicitly.}

\item{w_perp, w_par, w_angle}{Non-negative distance weights (each
default 1). Used for both grid generation and entropy computation.}

\item{verbose}{Logical; if \code{TRUE} (default), prints the estimated
parameters.}
}
\value{
A \code{tc_estimate} S3 object (list) with elements:
\describe{
\item{eps}{Numeric: estimated optimal eps value.}
\item{min_lns}{Integer: estimated min_lns value
(ceiling of mean neighbourhood size at optimal eps, + 1).}
\item{w_perp,w_par,w_angle}{Numeric: weight values used
(input values, not estimated).}
\item{entropy_df}{\code{data.frame} with columns \code{eps} and \code{entropy}
for the full grid.}
}
}
\description{
Provides a data-driven starting point for \code{eps} and \code{min_lns} by
minimizing the entropy of epsilon-neighbourhood sizes across a grid
of candidate eps values (Paper Section 4.4). This is an optional
helper — the optimal values are typically found by trial and error.
}
\details{
The algorithm works in two stages:
\enumerate{
\item \strong{Grid generation} (when \code{eps_grid = NULL}): A random sample of
\code{sample_size} segments is drawn. All pairwise distances within the
sample are computed. The grid spans 50 equally spaced values from
the 5th to 95th percentile of these distances.
\item \strong{Entropy computation}: For each candidate eps, the neighbourhood
size of each sampled segment is counted. The entropy of these sizes
(normalized to a probability distribution) is computed. The eps with
minimum entropy is selected. Ties are broken by choosing the smallest
eps.
}

The estimated \code{min_lns} is \verb{ceiling(mean neighbourhood size at optimal eps) + 1}.
}
\note{
This function does not break the pipe chain — it accepts a
\code{tc_partitions} object and returns a \code{tc_estimate} object (not a
workflow object). Use \code{result$eps} and \code{result$min_lns} to extract
the values for \code{\link[=tc_cluster]{tc_cluster()}}.
}
\examples{
trj <- tc_trajectories(traclus_toy, traj_id = "traj_id",
                        x = "x", y = "y", coord_type = "euclidean")
parts <- tc_partition(trj)
\donttest{
est <- tc_estimate_params(parts)
print(est)
}
}
\seealso{
Other helper functions: 
\code{\link{tc_read_hurdat2}()}
}
\concept{helper functions}
