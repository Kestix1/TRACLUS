% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tc_represent.R
\name{tc_represent}
\alias{tc_represent}
\title{Generate representative trajectories for clusters}
\usage{
tc_represent(x, gamma = 1, min_lns = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A \code{tc_clusters} object created by \code{\link[=tc_cluster]{tc_cluster()}}.}

\item{gamma}{Positive numeric smoothing parameter: minimum distance
between consecutive waypoints (default 1). Unit: meters for
\code{method = "haversine"} or \code{"projected"}, coordinate units for
\code{method = "euclidean"}.}

\item{min_lns}{Positive integer or \code{NULL} (default). Minimum number
of crossing segments required to generate a waypoint. If \code{NULL},
inherits from \code{x$params$min_lns} (clustering value). Override for
advanced use.}

\item{verbose}{Logical; if \code{TRUE} (default), prints an informative
summary.}
}
\value{
A \code{tc_representatives} S3 object (list) with elements:
\describe{
\item{segments}{\code{data.frame} — own copy of segment data with updated
\code{cluster_id} (renumbered, failed clusters degraded to noise).}
\item{representatives}{\code{data.frame} with columns \code{cluster_id}
(integer), \code{point_id} (integer, 1-based), \code{rx}, \code{ry} (numeric
waypoint coordinates in original format).}
\item{clusters}{Reference to the input \code{tc_clusters} object
(unmodified, pre-cleanup IDs for traceability).}
\item{n_clusters}{Integer number of clusters with valid representatives.}
\item{n_noise}{Integer number of noise segments (updated after cleanup).}
\item{params}{List with \code{min_lns} and \code{gamma} used for representation.}
\item{coord_type}{Coordinate type inherited from input.}
\item{method}{Distance method inherited from input.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}
}
\details{
Computes a representative trajectory for each cluster using the
sweep-line algorithm (Paper Figure 15). The representative captures
the "average path" of all line segments in the cluster.

For geographic data (\code{coord_type = "geographic"}), segment coordinates
are locally projected to meters using equirectangular approximation
before the sweep-line runs, then transformed back. This ensures
gamma operates in meters, consistent with eps.

Clusters that produce fewer than 2 waypoints are degraded to noise.
The remaining cluster IDs are renumbered (1..K) and the segments
data.frame is updated accordingly.

\strong{Trajectory diversity check (deviation from paper):} The sweep-line
checks segment density (\code{count >= min_lns}, per Paper Figure 15) AND
additionally requires at least 2 distinct trajectories among the
active segments at each waypoint position. This prevents degenerate
representatives from consecutive segments of a single trajectory
whose shared characteristic points create artificial count peaks
via entry-before-exit tie-breaking. The check only affects results
when \code{min_lns < 3}; at higher values, the segment density threshold
is already sufficient. See \code{vignette("algorithm-details")} for a
detailed explanation.
}
\examples{
trj <- tc_trajectories(traclus_toy,
  traj_id = "traj_id",
  x = "x", y = "y", coord_type = "euclidean"
)
parts <- tc_partition(trj)
\donttest{
clust <- tc_cluster(parts, eps = 25, min_lns = 3)
repr <- tc_represent(clust)
print(repr)
}
}
\seealso{
Other workflow functions: 
\code{\link{plot.TRACLUS}},
\code{\link{print.TRACLUS}},
\code{\link{summary.TRACLUS}},
\code{\link{tc_cluster}()},
\code{\link{tc_leaflet}()},
\code{\link{tc_partition}()},
\code{\link{tc_plot}()},
\code{\link{tc_traclus}()},
\code{\link{tc_trajectories}()}
}
\concept{workflow functions}
