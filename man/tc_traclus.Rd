% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tc_traclus.R
\name{tc_traclus}
\alias{tc_traclus}
\title{Run the complete TRACLUS algorithm}
\usage{
tc_traclus(
  x,
  eps,
  min_lns,
  gamma = 1,
  repr_min_lns = NULL,
  w_perp = 1,
  w_par = 1,
  w_angle = 1,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{tc_trajectories} object created by \code{\link[=tc_trajectories]{tc_trajectories()}}.}

\item{eps}{Positive numeric distance threshold for clustering.
Unit: meters for \code{method = "haversine"} or \code{"projected"}, coordinate
units for \code{method = "euclidean"}. \strong{Required} — no default.}

\item{min_lns}{Positive integer (>= 1). Used for both clustering
(minimum neighbourhood size / trajectory cardinality) and
representation (minimum segment density for waypoints).
\strong{Required} — no default.}

\item{gamma}{Positive numeric smoothing parameter for representative
generation (default 1). Minimum distance between consecutive
waypoints.}

\item{repr_min_lns}{Optional positive integer. If provided, overrides
\code{min_lns} for the representation step only (power-user option).
Corresponds to the \code{min_lns} parameter of \code{\link[=tc_represent]{tc_represent()}}.
If \code{NULL} (default), the clustering \code{min_lns} is used for both phases.}

\item{w_perp, w_par, w_angle}{Non-negative weights for perpendicular,
parallel, and angle distance components (each default 1).}

\item{verbose}{Logical; if \code{TRUE} (default), prints informative
messages after each step. Passed through to all three sub-functions.}
}
\value{
A \code{tc_traclus} object (inherits from \code{tc_representatives}).
The full reference chain is preserved:
\code{result$clusters$partitions$trajectories}.
}
\description{
Convenience wrapper that executes all three TRACLUS steps in sequence:
partitioning (\code{\link[=tc_partition]{tc_partition()}}), clustering (\code{\link[=tc_cluster]{tc_cluster()}}), and
representative trajectory generation (\code{\link[=tc_represent]{tc_represent()}}).
}
\details{
This wrapper is equivalent to:

\if{html}{\out{<div class="sourceCode">}}\preformatted{x |> tc_partition() |>
  tc_cluster(eps, min_lns, w_perp, w_par, w_angle) |>
  tc_represent(gamma, min_lns)
}\if{html}{\out{</div>}}

For iterative parameter tuning, use the step-by-step approach instead:
partition once with \code{\link[=tc_partition]{tc_partition()}}, then re-cluster the same
partitions object with different \code{eps}/\code{min_lns} values.
}
\examples{
trj <- tc_trajectories(traclus_toy, traj_id = "traj_id",
                        x = "x", y = "y", coord_type = "euclidean")
\donttest{
result <- tc_traclus(trj, eps = 25, min_lns = 3)
print(result)
}
}
\seealso{
Other workflow functions: 
\code{\link{plot.TRACLUS}},
\code{\link{print.TRACLUS}},
\code{\link{summary.TRACLUS}},
\code{\link{tc_cluster}()},
\code{\link{tc_leaflet}()},
\code{\link{tc_partition}()},
\code{\link{tc_plot}()},
\code{\link{tc_represent}()},
\code{\link{tc_trajectories}()}
}
\concept{workflow functions}
