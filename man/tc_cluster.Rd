% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tc_cluster.R
\name{tc_cluster}
\alias{tc_cluster}
\title{Cluster line segments using density-based clustering}
\usage{
tc_cluster(x, eps, min_lns, w_perp = 1, w_par = 1, w_angle = 1, verbose = TRUE)
}
\arguments{
\item{x}{A \code{tc_partitions} object created by \code{\link[=tc_partition]{tc_partition()}}.}

\item{eps}{Positive numeric distance threshold. Segments within \code{eps}
distance are considered neighbours. Unit: meters for
\code{method = "haversine"} or \code{"projected"}, coordinate units for
\code{method = "euclidean"}.
\strong{Required} — no default. Use trial and error or
\code{\link[=tc_estimate_params]{tc_estimate_params()}} for guidance.}

\item{min_lns}{Positive integer (>= 1). Minimum neighbourhood size
for a segment to be a core segment in DBSCAN, AND minimum number
of distinct source trajectories for a cluster to be retained.
\strong{Required} — no default.}

\item{w_perp}{Non-negative weight for perpendicular distance (default 1).}

\item{w_par}{Non-negative weight for parallel distance (default 1).}

\item{w_angle}{Non-negative weight for angle distance (default 1).}

\item{verbose}{Logical; if \code{TRUE} (default), prints an informative
summary after clustering.}
}
\value{
A \code{tc_clusters} S3 object (list) with elements:
\describe{
\item{segments}{\code{data.frame} with columns \code{traj_id}, \code{seg_id}, \code{sx},
\code{sy}, \code{ex}, \code{ey}, \code{cluster_id} (integer, \code{NA} for noise).}
\item{cluster_summary}{\code{data.frame} with per-cluster statistics:
\code{cluster_id}, \code{n_segments}, \code{n_trajectories}.}
\item{n_clusters}{Integer number of clusters (excluding noise).}
\item{n_noise}{Integer number of noise segments.}
\item{params}{List with \code{eps}, \code{min_lns}, \code{w_perp}, \code{w_par}, \code{w_angle}.}
\item{partitions}{Reference to the input \code{tc_partitions} object.}
\item{coord_type}{Coordinate type inherited from input.}
\item{method}{Distance method inherited from input.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}
}
\details{
Applies a modified DBSCAN algorithm to group similar line segments
into clusters. Segments that are not dense enough are classified as
noise (cluster_id = NA). After clustering, a trajectory cardinality
check removes clusters whose segments originate from fewer than
\code{min_lns} distinct trajectories (Paper Section 4.3).

The weighted distance between two segments is:
\code{dist = w_perp * d_perp + w_par * d_par + w_angle * d_angle}
(Paper Section 2.3). Early termination skips remaining components
when the accumulated distance already exceeds \code{eps}.

The DBSCAN modification from the paper: noise segments CAN be absorbed
into a cluster during expansion, but they do NOT trigger further
expansion (Paper Figure 12, Lines 23-26).

\strong{Choosing \code{min_lns}:} The paper recommends \code{min_lns} =
avg|N_eps(L)| + 1 to 3, typically yielding values of 5–9 (Section
4.4). Use \code{\link[=tc_estimate_params]{tc_estimate_params()}} for a data-driven starting point.
Values below 3 can lead to clusters dominated by a single trajectory
and degenerate representative trajectories — see \code{\link[=tc_represent]{tc_represent()}}
details for the trajectory diversity check that mitigates this.
}
\examples{
trj <- tc_trajectories(traclus_toy,
  traj_id = "traj_id",
  x = "x", y = "y", coord_type = "euclidean"
)
parts <- tc_partition(trj)
\donttest{
clust <- tc_cluster(parts, eps = 10, min_lns = 3)
print(clust)
}
}
\seealso{
Other workflow functions: 
\code{\link{plot.TRACLUS}},
\code{\link{print.TRACLUS}},
\code{\link{summary.TRACLUS}},
\code{\link{tc_leaflet}()},
\code{\link{tc_partition}()},
\code{\link{tc_plot}()},
\code{\link{tc_represent}()},
\code{\link{tc_traclus}()},
\code{\link{tc_trajectories}()}
}
\concept{workflow functions}
