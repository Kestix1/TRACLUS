% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.tc_estimate}
\alias{plot.tc_estimate}
\title{Plot method for tc_estimate objects}
\usage{
\method{plot}{tc_estimate}(x, ...)
}
\arguments{
\item{x}{A \code{tc_estimate} object from \code{\link[=tc_estimate_params]{tc_estimate_params()}}.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
Displays the entropy curve across the eps grid for visual inspection.
Use the shape of the curve — pronounced drops or elbows — to choose
an appropriate \code{eps} value.
}
\examples{
\donttest{
trj <- tc_trajectories(traclus_toy, traj_id = "traj_id",
                       x = "x", y = "y", coord_type = "euclidean")
parts <- tc_partition(trj)
est <- tc_estimate_params(parts)
plot(est)
}
}
