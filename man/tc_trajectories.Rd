% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tc_trajectories.R
\name{tc_trajectories}
\alias{tc_trajectories}
\title{Validate and prepare trajectory data}
\usage{
tc_trajectories(
  data,
  traj_id,
  x = NULL,
  y = NULL,
  coord_type = NULL,
  method = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame}, \code{tibble}, or \code{sf} object containing trajectory
point data in tidy format (one row per point).}

\item{traj_id}{Character string naming the column that identifies
trajectories. Values can be character, integer, or factor; they are stored
internally as character.}

\item{x}{Character string naming the column with x-coordinates (longitude
for geographic data). Required for \code{data.frame} input; ignored for \code{sf}
input (extracted from geometry).}

\item{y}{Character string naming the column with y-coordinates (latitude
for geographic data). Required for \code{data.frame} input; ignored for \code{sf}
input (extracted from geometry).}

\item{coord_type}{Character string: \code{"euclidean"} or \code{"geographic"}.
Required for \code{data.frame} input. For \code{sf} input, inferred from the CRS.
Geographic data uses the convention x = longitude (-180 to 180),
y = latitude (-90 to 90).}

\item{method}{Character string specifying the distance calculation method.
Default \code{NULL} resolves to \code{"haversine"} for geographic data and
\code{"euclidean"} for euclidean data. Options for \code{coord_type = "geographic"}:
\describe{
\item{\code{"haversine"}}{(default) Exact great-circle distances in meters.
Most accurate, but slowest for large datasets.}
\item{\code{"projected"}}{Equirectangular projection to meters, then
euclidean distances. 5-10x faster than haversine with < 2\\% error
for regional datasets (e.g., hurricane basins, taxi data).
Recommended for most geographic use cases.}
\item{\code{"euclidean"}}{Raw degree values treated as cartesian coordinates.
Only for paper replication mode.}
}}

\item{verbose}{Logical; if \code{TRUE} (default), prints an informative summary
after validation.}
}
\value{
A \code{tc_trajectories} S3 object (list) with elements:
\describe{
\item{data}{\code{data.frame} with columns \code{traj_id} (character), \code{x}
(numeric), \code{y} (numeric), ordered by trajectory.}
\item{coord_type}{Resolved coordinate type (\code{"euclidean"} or
\code{"geographic"}).}
\item{method}{Resolved distance method (\code{"euclidean"},
\code{"haversine"}, or \code{"projected"}).}
\item{n_trajectories}{Integer count of valid trajectories.}
\item{n_points}{Integer total number of points.}
}
}
\description{
Validates input data, resolves coordinate types and distance methods, and
creates a \code{tc_trajectories} object for use in the TRACLUS pipeline. This is
the entry point for all TRACLUS analyses.
}
\details{
Validation is performed in four stages:
\enumerate{
\item \strong{Base checks}: Column existence, numeric and finite coordinates,
plausibility warnings (e.g., swapped lon/lat).
\item \strong{Filtering} (in order): (a) Remove rows with non-finite x/y or
NA traj_id. (b) Remove consecutive duplicate points within
trajectories. (c) Remove trajectories with fewer than 2 points.
\item \strong{Warnings}: Report what was removed, truncating IDs to the first
5 if many are affected.
\item \strong{Final check}: Error if fewer than 2 valid trajectories remain.
}
}
\examples{
# Euclidean example with built-in toy data
trj <- tc_trajectories(traclus_toy, traj_id = "traj_id",
                       x = "x", y = "y", coord_type = "euclidean")
print(trj)

# Geographic example
geo <- data.frame(
  id = rep(c("A", "B", "C"), each = 5),
  lon = c(-80,-78,-76,-74,-72, -82,-79,-76,-73,-70, -60,-58,-56,-54,-52),
  lat = c(25,26,27,28,29, 24,25,26,27,28, 30,31,32,33,34)
)
trj_geo <- tc_trajectories(geo, traj_id = "id", x = "lon", y = "lat",
                           coord_type = "geographic")

# sf input (coord_type inferred from CRS)
\donttest{
if (requireNamespace("sf", quietly = TRUE)) {
  geo_sf <- sf::st_as_sf(geo, coords = c("lon", "lat"), crs = 4326)
  trj_sf <- tc_trajectories(geo_sf, traj_id = "id")
}
}
}
\seealso{
Other workflow functions: 
\code{\link{plot.TRACLUS}},
\code{\link{print.TRACLUS}},
\code{\link{summary.TRACLUS}},
\code{\link{tc_cluster}()},
\code{\link{tc_leaflet}()},
\code{\link{tc_partition}()},
\code{\link{tc_plot}()},
\code{\link{tc_represent}()},
\code{\link{tc_traclus}()}
}
\concept{workflow functions}
