% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{.cpp_count_neighbours_multi_eps}
\alias{.cpp_count_neighbours_multi_eps}
\title{Count neighbourhood sizes for multiple eps candidates (C++)}
\usage{
.cpp_count_neighbours_multi_eps(pair_dists, n_sample, eps_grid)
}
\arguments{
\item{pair_dists}{NumericVector of length n*(n-1)/2 (triangular, row-major,
i < j ordering produced by \code{.cpp_compute_pairwise_dists()}).}

\item{n_sample}{Integer; number of sampled segments.}

\item{eps_grid}{NumericVector of eps candidates, sorted ascending.}
}
\value{
List with:
\item{mean_nb_sizes}{mean neighbourhood size per eps (length n_eps)}
\item{entropy_vals}{distribution entropy per eps (length n_eps)}
}
\description{
Replaces Phase 2 of \code{\link[=tc_estimate_params]{tc_estimate_params()}}: given a pre-computed triangular
distance matrix and a sorted grid of eps candidates, counts neighbourhood
sizes for every segment and every eps in a single O(n_pairs * log(n_eps)) pass.
}
\details{
For each pair (i,j) with distance d, binary search finds all eps candidates= d; the counts for segment i and j are incremented for those eps values.Self-inclusion (+1) is added at the end.
}
\keyword{internal}
