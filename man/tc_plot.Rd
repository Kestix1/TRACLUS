% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{tc_plot}
\alias{tc_plot}
\title{Plot TRACLUS objects}
\usage{
tc_plot(x, show_clusters = FALSE, show_points = TRUE, ...)
}
\arguments{
\item{x}{A TRACLUS workflow object (\code{tc_trajectories}, \code{tc_partitions},
\code{tc_clusters}, \code{tc_representatives}, or \code{tc_traclus}).}

\item{show_clusters}{Logical; passed to \code{\link[=plot.tc_representatives]{plot.tc_representatives()}} when
\code{x} is a \code{tc_representatives} or \code{tc_traclus} object. If \code{FALSE}
(default), cluster member segments are drawn in grey and only the
representative lines appear in colour. If \code{TRUE}, cluster segments are
drawn in full colour with representatives overlaid.}

\item{show_points}{Logical; passed to \code{\link[=plot.tc_partitions]{plot.tc_partitions()}} when \code{x} is a
\code{tc_partitions} object. If \code{TRUE} (default), characteristic points are
drawn as black X markers.}

\item{...}{Additional arguments passed to \code{\link[=plot]{plot()}} / \code{\link[=segments]{segments()}}.
Use to override standard parameters like \code{main}, \code{xlim}, \code{ylim}.}
}
\value{
\code{x}, invisibly.
}
\description{
Convenience wrapper around the S3 \code{plot()} methods for TRACLUS objects.
Equivalent to calling \code{plot(x, ...)} directly, but discoverable via
\code{?tc_plot} and RStudio's F1 help.
}
\details{
All plot methods use the viridis colour palette (via
\code{\link[viridisLite:viridis]{viridisLite::viridis()}}) for colourblind-safe display and accept \code{...}
for standard plot parameters (\code{main}, \code{xlim}, \code{ylim}, \code{cex}, etc.).

This function calls \code{plot(x, ...)} with S3 dispatch based on the class of
\code{x}. Use \code{tc_plot()} when you want RStudio help lookup (F1) to work, or
\code{plot()} for standard R syntax — both produce identical results.
}
\examples{
trj <- tc_trajectories(traclus_toy, traj_id = "traj_id",
                       x = "x", y = "y", coord_type = "euclidean")
tc_plot(trj)  # same as plot(trj)

}
\seealso{
\code{\link[=tc_leaflet]{tc_leaflet()}} for interactive Leaflet maps (geographic data
only).

Other workflow functions: 
\code{\link{plot.TRACLUS}},
\code{\link{print.TRACLUS}},
\code{\link{summary.TRACLUS}},
\code{\link{tc_cluster}()},
\code{\link{tc_leaflet}()},
\code{\link{tc_partition}()},
\code{\link{tc_represent}()},
\code{\link{tc_traclus}()},
\code{\link{tc_trajectories}()}
}
\concept{workflow functions}
