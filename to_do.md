# TRACLUS Package — To Do

## Open

| Punkt | Datei(en) |
|-------|-----------|
| Doku: sf-Anforderung POINT-Geometrie in `@details` aufnehmen (inkl. Hinweis auf `sf::st_cast("POINT")`) | `R/tc_trajectories.R` |
| Doku: sf-Anforderung CRS muss gesetzt sein in `@details` aufnehmen (inkl. Hinweis auf `sf::st_set_crs()`) | `R/tc_trajectories.R` |
| Doku: Verhalten bei Z/M-Dimensionen (werden gedroppt) in `@details` dokumentieren | `R/tc_trajectories.R` |
| Vignette: sf-Workflow in mindestens einer Vignette erwähnen | `vignettes/` |